# PharmaNetwork currently uses the checked-in Firebase web application configuration
# for the AI Studio development environment.
#
# Production/staging environment configuration will be introduced during the
# infrastructure batch. Do not place service-account credentials or other
# backend secrets in VITE_* variables or frontend source code.

# AI Studio may set this development-only flag to reduce preview flicker.
DISABLE_HMR="false"
